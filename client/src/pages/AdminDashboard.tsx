import DashboardLayout from "@/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { trpc } from "@/lib/trpc";
import { ArrowLeft, Loader2, Plus, Search, Users, Weight } from "lucide-react";
import { useState } from "react";
import { Link } from "wouter";

export default function AdminDashboard() {
  const [search, setSearch] = useState("");
  const [open, setOpen] = useState(false);
  const [form, setForm] = useState({ name: "", phone: "", password: "" });
  const brokers = trpc.brokers.list.useQuery({ search }, { staleTime: 10_000 });
  const create = trpc.brokers.create.useMutation({ onSuccess: () => { setOpen(false); setForm({ name: "", phone: "", password: "" }); brokers.refetch(); } });

  return <DashboardLayout><div className="space-y-8"><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="mb-2 text-sm font-bold text-[#ad7d3f]">إدارة الحسابات</p><h1 className="font-serif text-4xl">جميع العملاء</h1><p className="mt-2 text-[#8d7c6a]">تابع حسابات الأبونيهات وحركاتهم من مكان واحد.</p></div><Dialog open={open} onOpenChange={setOpen}><DialogTrigger asChild><Button className="rounded-xl bg-[#201b17] text-white hover:bg-[#3a3028]"><Plus size={18} className="ml-2" />إضافة أبونيه</Button></DialogTrigger><DialogContent dir="rtl" className="rounded-2xl sm:max-w-md"><DialogHeader><DialogTitle className="font-serif text-2xl">إضافة أبونيه جديد</DialogTitle></DialogHeader><form className="space-y-4" onSubmit={e => { e.preventDefault(); create.mutate(form); }}><div className="space-y-2"><Label>الاسم</Label><Input required value={form.name} onChange={e => setForm({ ...form, name: e.target.value })} /></div><div className="space-y-2"><Label>رقم الهاتف</Label><Input required dir="ltr" inputMode="tel" value={form.phone} onChange={e => setForm({ ...form, phone: e.target.value })} /></div><div className="space-y-2"><Label>كلمة المرور</Label><Input required minLength={8} dir="ltr" type="password" value={form.password} onChange={e => setForm({ ...form, password: e.target.value })} /></div>{create.error && <p className="text-sm text-red-600">تعذر إنشاء الحساب. تأكد أن رقم الهاتف غير مستخدم.</p>}<Button disabled={create.isPending} className="w-full rounded-xl bg-[#ad7d3f] text-white hover:bg-[#94662f]">{create.isPending ? <Loader2 className="animate-spin" /> : "حفظ الحساب"}</Button></form></DialogContent></Dialog></div><div className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-[#e5ded4] bg-white p-5 shadow-sm"><div className="mb-4 flex items-center justify-between"><span className="text-sm text-[#8d7c6a]">عدد الأبونيهات</span><Users size={19} className="text-[#ad7d3f]" /></div><p className="font-serif text-3xl">{brokers.data?.length ?? 0}</p></div><div className="rounded-2xl border border-[#e5ded4] bg-white p-5 shadow-sm"><div className="mb-4 flex items-center justify-between"><span className="text-sm text-[#8d7c6a]">إجمالي الوزن</span><Weight size={19} className="text-[#ad7d3f]" /></div><p className="font-serif text-3xl">{brokers.data?.reduce((sum, b) => sum + Number(b.totalWeight), 0).toFixed(3)} <span className="text-sm font-sans text-[#8d7c6a]">جرام</span></p></div><div className="rounded-2xl border border-[#e5ded4] bg-white p-5 shadow-sm"><div className="mb-4 flex items-center justify-between"><span className="text-sm text-[#8d7c6a]">إجمالي النقدية</span><span className="text-lg text-[#ad7d3f]">ج.م</span></div><p className="font-serif text-3xl">{brokers.data?.reduce((sum, b) => sum + Number(b.totalCash), 0).toFixed(2)}</p></div></div><div className="rounded-2xl border border-[#e5ded4] bg-white shadow-sm"><div className="flex items-center justify-between border-b border-[#eee7de] p-5"><div><h2 className="font-serif text-xl">حسابات الأبونيهات</h2><p className="mt-1 text-sm text-[#8d7c6a]">اختر حسابًا لعرض دفتر الشغل.</p></div><div className="relative w-64"><Search className="absolute right-3 top-2.5 text-[#a18f7d]" size={17} /><Input value={search} onChange={e => setSearch(e.target.value)} placeholder="ابحث بالاسم أو الهاتف" className="pr-9" /></div></div>{brokers.isLoading ? <div className="flex justify-center p-14"><Loader2 className="animate-spin text-[#ad7d3f]" /></div> : brokers.error ? <div className="p-14 text-center"><p className="font-serif text-xl">تعذر تحميل الحسابات</p><p className="mt-2 text-sm text-red-600">تحقق من الاتصال ثم حاول مرة أخرى.</p><Button variant="outline" className="mt-5 rounded-xl" onClick={() => brokers.refetch()}>إعادة المحاولة</Button></div> : brokers.data?.length ? <div className="overflow-x-auto"><table className="w-full text-right text-sm"><thead className="bg-[#fcfaf7] text-xs text-[#8d7c6a]"><tr><th className="px-5 py-4 font-medium">الاسم</th><th className="px-5 py-4 font-medium">رقم الهاتف</th><th className="px-5 py-4 font-medium">إجمالي الوزن</th><th className="px-5 py-4 font-medium">إجمالي النقدية</th><th className="px-5 py-4" /></tr></thead><tbody>{brokers.data.map(b => <tr key={b.id} className="border-t border-[#eee7de] transition hover:bg-[#fcfaf7]"><td className="px-5 py-4 font-semibold">{b.name}</td><td className="px-5 py-4 text-left" dir="ltr">{b.phone}</td><td className="px-5 py-4">{Number(b.totalWeight).toFixed(3)} جرام</td><td className="px-5 py-4">{Number(b.totalCash).toFixed(2)} ج.م</td><td className="px-5 py-4"><Link href={`/admin/brokers/${b.id}`} className="inline-flex items-center gap-1 font-bold text-[#ad7d3f]">فتح الحساب <ArrowLeft size={15} /></Link></td></tr>)}</tbody></table></div> : <div className="p-14 text-center"><p className="font-serif text-xl">لا توجد حسابات بعد</p><p className="mt-2 text-sm text-[#8d7c6a]">ابدأ بإضافة أول أبونيه إلى النظام.</p></div>}</div></div></DashboardLayout>;
}
